import React from 'react';
import './Spinner.css'; // Make sure to import or define styles for the spinner

const Spinner = () => (
  <div className="spinner-container">
      <div className="spinner"></div>
        </div>
);

export default Spinner;
